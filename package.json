{
  "scripts": {
    "lint": "solhint \"{src,utils,scripts}/**/*.sol\"",
    "lint:fix": "solhint \"{src,utils,scripts}/**/*.sol\" --fix",
    "vyper:format": "poetry shell && poetry install && mamushi vyper_contracts",
    "solidity:format": "prettier --write \"{src,utils,scripts}/**/*.sol\""

  },
  "devDependencies": {
    "prettier": "^2.8.8",
    "prettier-plugin-solidity": "^1.1.3",
    "solhint": "^3.4.1"
  }
}
